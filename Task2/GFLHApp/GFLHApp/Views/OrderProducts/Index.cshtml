@model IEnumerable<GFLHApp.Models.OrderProducts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductQuantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvoiceNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductQuantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Products.ProductsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Orders.OrdersId)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InvoiceNumber)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderProductsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderProductsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderProductsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
